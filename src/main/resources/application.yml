spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Spring_Security_project
    username: postgres
    password: 2805
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081